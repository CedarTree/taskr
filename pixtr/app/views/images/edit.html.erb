<%= form_for [@gallery, @image] do |form| %>

 <!-- url: "/galleries/#{@gallery.id}/images"-->
  
    <%= form.label :url, "URL" %>
    <%= form.text_field :url %>
    <%= form.submit "Edit an image" %>
 <% end %>

