<%= form_for [@gallery, @image] do |form| %>

 <!-- url: "/galleries/#{@gallery.id}/images"-->
    <%= render "error_messages", target: @image%>
    <%= form.label :url, "URL" %>
    <%= form.text_field :url %>
    <%= form.submit "Create a image" %>
 <% end %>

